name: openvino-hello-query-sample # you probably want to 'snapcraft register <name>'
base: core20 # the base snap is the execution environment for this snap
version: '1.0' # just for humans, typically '1.2+git' or '1.3.2'
summary: OpenVINO sample application # 79 char long summary
description: |
  OpenVINO sample application.

grade: stable # must be 'stable' to release into candidate/stable channels
confinement: strict # use 'strict' once you have the right plugs and slots

parts:
  cmake-build-part:
    plugin: cmake
    source: ${SNAPCRAFT_PROJECT_DIR}
    source-type: local
    build-environment:
    - LC_ALL: C.UTF-8
    - LANG: C.UTF-8
    cmake-parameters:
    - -DCMAKE_BUILD_TYPE=Release
    - -DCMAKE_INSTALL_PREFIX=/usr
    build-packages:
    - libgflags-dev
    stage-packages:
    - libgflags2.2
    stage-snaps:
    - openvino/latest/beta
    build-snaps:
    - cmake
    - openvino/latest/beta

plugs:
  openvino-2022-2-0:
    interface: content
    content: openvino-2022-2-0
    target: $SNAP/extra-libs

apps:
  hello-query-device:
    command: usr/bin/hello_query_sample
    # environment:
    #   LD_LIBRARY_PATH: $LD_LIBRARY_PATH:$SNAP/usr/lib
